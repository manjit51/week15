spring:
   datasource:
      username: pet_stores
      password: pet_stores
      url: jdbc:mysql://localhost:3306/pet_stores
      
   jpa:
      hibernate:
         ddl-auto: update
      show-sql: true
      defer-datasource-initialization: true
      
   sql:
      init:
         mode: never